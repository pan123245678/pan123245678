在linux国产的设备中，尽量能保证网卡、内存、硬盘、CPU在一个Die上，这样可以极大减少数据跨Die传输带来的性能损耗。

## 查看NUMA
``` shell
lscpu
```

## 查看网卡在哪个Die
``` shell
lspci | grep Eth
```
输出的地址中，前4位表示在那个Die上

## 查看磁盘在哪个Die
``` shell
lspci | grep Non
```
输出的地址中，前4位表示在那个Die上

## 查看每个Die上的内存
``` shell
numactl -H
```